{
    "trailingSlash": "auto",
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["/*.{png,jpg,gif,woff,woff2}", "/assets/*"]
      },
      "routes": [
        {
          "route": "/login",
          "redirect": "/.auth/login/github"
        }
      ]
}